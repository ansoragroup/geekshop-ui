Reinitialize session context after context compaction.

Instructions:
1. Read CLAUDE.md for all project rules and patterns
2. Read .claude/PLAN.md for current implementation status
3. Read .claude/scratchpad.md for any in-progress work context
4. Read src/components/index.ts to see all current component exports
5. Read src/hooks/index.ts for available hooks
6. Run `git log --oneline -10` to see recent changes
7. Run `git status` to see current working state

After reading, summarize:
- Current project state (what phase we're in)
- Any in-progress work from scratchpad
- Key rules to remember (especially visual QA, container queries, forwardRef)

This command should be run after every context compaction to restore awareness.
